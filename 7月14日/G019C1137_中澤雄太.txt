# 問19
SELECT COUNT(*)
FROM 受注;

# 問20
SELECT SUM(受注合計)
FROM 受注;

# 問21
SELECT AVG(受注合計)
FROM 受注;

# 問22
SELECT MAX(受注合計)
FROM 受注;

# 問23
SELECT MIN(受注合計)
FROM 受注;

# 問24
SELECT 受注年月日, COUNT(*), SUM(受注合計)
FROM 受注
GROUP BY 受注年月日;

# 問25